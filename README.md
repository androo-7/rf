# rf -- Run a command From a different directory.

small bash script i made to run commands from some dir without cd-ing there.

usage:  
```
rf [-h] <file/path/of/dir> <command>
```  
options:  
```
    -h — show help
```
